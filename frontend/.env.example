# Base URL of your Railway backend — used for all /api/* calls
VITE_API_URL=https://your-backend.up.railway.app/api

# Same Railway URL (without /api) — used for /static/* image and file URLs
VITE_BACKEND_URL=https://your-backend.up.railway.app
